<?php

namespace Tests;

use Illuminate\Foundation\Testing\TestCase as BaseTestCase;

abstract class TestCase extends BaseTestCase
{
    //Arrange - Act - Assert
    // 1. Arrange (set up the environment, create necessary objects, and prepare any data needed for the test
    // 2. Act (execute the code being tested, such as calling a method or making an API request
    // 3. Assert (verify that the actual results match the expected results)
}
